<%= form_for(@farmer) do |f| %>
  <% if @farmer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@farmer.errors.count, "error") %> prohibited this farmer from being saved:</h2>

      <ul>
      <% @farmer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
		</br>
    <%= f.text_field :email, class: "form-control", :placeholder => "Epost"%>
  </div>
  <div class="field">
		</br>
    <%= f.text_field :name, class: "form-control", :placeholder => "Navn"%>
  </div>
  <div class="field">
		</br>
    <%= f.text_field :backup, class: "form-control", :placeholder => "Avløser"%>
  </div>
  <div class="field">
		</br>
    <%= f.password_field :password, class: "form-control", :placeholder => "Passord"%>
  </div>
  <div class="field">
		</br>
    <%= f.text_field :phone, class: "form-control", :placeholder => "Telefon nummer"%>
  </div>
  <div class="actions">
		</br>
    <%= f.submit "Oppdater", class: "btn btn-large btn-primary"%>
<%= link_to 'Tilbake', @farmer, class: "btn btn-large btn-default"%>
  </div>
<% end %>
